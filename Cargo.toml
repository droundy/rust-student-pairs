[package]
name = "rust-student-pairs"
version = "0.1.0"
authors = ["droundy <roundyd@physics.oregonstate.edu>"]

build = "build.rs"

[dependencies]
actix-web = "0.7"
serde = "1.0"
serde_derive = "1.0"
askama = "0.7"

[build-dependencies]
askama = "0.7"
